- defaults:
    link:
      create: true
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.vimrc:
    ~/.zshrc:
    ~/bin:
    ~/.gitconfig:
    ~/.gitignore:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
